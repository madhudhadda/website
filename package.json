{
  "name": "website",
  "version": "1.0.0",
  "description": "HealthLabh - Healthcare website with Vercel Web Analytics",
  "private": true,
  "scripts": {
    "dev": "npx http-server -p 3000",
    "build": "echo 'Build complete - static site'",
    "lint": "echo 'No linter configured for static HTML'"
  },
  "dependencies": {
    "@vercel/analytics": "^1.1.1"
  }
}
